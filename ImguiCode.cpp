#include "ImguiCode.h"

// 例: 将来的にクラスとしてまとめる場合のラッパー実装
void Imgui::Initialize() {
    // ここで ImGui::CreateContext() や 各種 Init を呼ぶ
}

void Imgui::Finalize() {
    // ここで ImGui_ImplDX12_Shutdown() などを呼ぶ
}